#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../wordcut/bin/wordcut" "$@"
  ret=$?
else 
  node  "$basedir/../wordcut/bin/wordcut" "$@"
  ret=$?
fi
exit $ret
